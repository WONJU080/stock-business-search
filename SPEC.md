# 股票业务数据库 · 格式规范

本数据库用于保存「AI 分析股票财报后提取的在营业务信息」，供后续程序检索、更新与查重。
全部文件采用 **UTF-8** 编码的**纯文本**。

## 1. 目录结构

```
db/
├── index.txt          # 总揽文件：记录所有已分析股票
└── stocks/            # 明细目录：每个已分析股票独占一个文件
    ├── 600519.txt
    └── 000001.txt
```

- 文件名为 `{6位A股代码}.txt`，代码**必须补零至 6 位**（如 `000001.txt`）。
- 一个股票一个文件，互不交叉。

## 2. 总揽文件 `db/index.txt`

用途：让程序快速确认某股票是否已被分析，避免重复分析。

### 规则
- 每行一条记录，对应一个已分析股票。
- 字段以 `|` 分隔，顺序固定，共 6 列。
- 以 `#` 开头的行是注释，程序忽略。
- 不允许出现空行记录（空行仅允许在文件末尾）。

### 字段顺序

```
代码|名称|报告期|分析日期|更新时间|文件路径
```

| 列 | 含义 | 格式 | 说明 |
|----|------|------|------|
| 代码 | A股代码 | 6 位数字字符串 | 补零，如 `000001` |
| 名称 | 股票简称 | 文本 | 不含 `|` |
| 报告期 | 财报报告期 | `YYYY-MM-DD` | 如 `2025-12-31`；未知填 `-` |
| 分析日期 | 首次分析日期 | `YYYY-MM-DD` | |
| 更新时间 | 最后更新日期 | `YYYY-MM-DD` | |
| 文件路径 | 明细文件相对路径 | 相对 `db/` | 如 `stocks/600519.txt` |

### 示例

```
# 股票分析总揽
# 字段: 代码|名称|报告期|分析日期|更新时间|文件路径
000001|平安银行|2025-12-31|2026-09-13|2026-09-14|stocks/000001.txt
600519|贵州茅台|2025-12-31|2026-09-14|2026-09-14|stocks/600519.txt
```

## 3. 股票明细文件 `db/stocks/{代码}.txt`

每个文件包含两部分：**元数据区**（键值对）+ **区块区**（内容段）。

### 3.1 元数据区

- 位于文件开头，每行 `键 = 值`（`=` 前后各一个空格）。
- 键固定为以下 5 个，顺序固定：

```
代码 = 600519
名称 = 贵州茅台
报告期 = 2025-12-31
分析日期 = 2026-09-14
更新时间 = 2026-09-14
```

| 键 | 含义 | 格式 |
|----|------|------|
| 代码 | A股代码 | 6 位补零 |
| 名称 | 股票简称 | 文本 |
| 报告期 | 财报报告期 | `YYYY-MM-DD`，未知填 `-` |
| 分析日期 | 首次分析日期 | `YYYY-MM-DD` |
| 更新时间 | 最后更新日期 | `YYYY-MM-DD` |

### 3.2 区块区

- 区块以 `[区块名]` 独占一行开始，内容延续到下一个 `[区块名]` 或文件末尾。
- 现有两个区块，顺序固定：`[在营业务]` 在前，`[关键词]` 在后。

#### `[在营业务]`（核心，检索主体）

- 内容为 AI 从财报中提取的**全部在营业务信息**，以**不概括**的方式写成**一长段**文字。
- 必须完整保留所有业务细节，不得省略、不得概括成一句话。
- 段内不使用空行；如因内容过长需换行，直接续行即可，程序会合并为整段。
- 该区块是关键词检索的**主检索源**。

#### `[关键词]`

- 逗号分隔的关键词/标签，用于辅助检索与人工浏览。
- 建议由 AI 从业务内容中提炼，可单行；多关键词用 `, ` 分隔。

### 3.3 完整示例

```
代码 = 600519
名称 = 贵州茅台
报告期 = 2025-12-31
分析日期 = 2026-09-14
更新时间 = 2026-09-14

[在营业务]
贵州茅台主营业务为茅台酒及系列酒的生产与销售。公司主导产品为53度飞天茅台酒，
同时经营茅台1935、汉酱、赖茅等系列酒产品，并涉足酒类销售、电子商务等配套业务。

[关键词]
白酒, 茅台酒, 酱香型白酒, 系列酒, 酒类销售
```

## 4. 一致性约定

- 明细文件与总揽文件中的 `代码/名称/报告期/分析日期/更新时间` **必须完全一致**。
- 更新已分析股票时：覆盖明细文件，并在总揽中更新 `更新时间`（及报告期如有变化）。
- 删除明细文件时，须同步删除总揽中对应行。
- 所有工具脚本（`tools/db.py`）负责维持上述一致性，AI 与程序应通过工具读写，勿手工改动。
